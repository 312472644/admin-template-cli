const { logSuccess, logError, logInfo, logWarning } = require('../utils/index.js');

const implementCreateCmd = options => {
  console.log('options', options);
};

module.exports = { implementCreateCmd };
